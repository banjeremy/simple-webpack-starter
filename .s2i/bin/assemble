#!/bin/bash

# exec default assemble script
/usr/libexec/s2i/assemble
rc=$?

if [ $rc -eq 0 ]; then # assemble was successful
    echo "Running npm build..."
    npm run build

    echo "Archiving build..."
    tar -C dist -cvzf dist.tar.gz .
fi

exit $rc
